# Plan: task-muguka57

**Project:** `PRJ-8960`  
**Task ID:** `task-muguka57`  
**Repo:** `prj-8999-task-muguka57`  
**Progress:** 1/4 subtasks done

## Summary

Декомпозиция задачи: CLI-обёртка над Git через subprocess с разделением на core-модуль, CLI-интерфейс и тесты.

## Subtasks

### ❌ 1. Инициализация структуры проекта

- **ID:** `sub-1`
- **Profile:** `20razrab1`
- **Status:** `failed`
- **Description:** Создать структуру репозитория: pyproject.toml (или setup.py), директории src/git_cli/ и tests/, файл README.md с описанием использования. Зафиксировать Python>=3.10 и зависимости (только stdlib).
- **Test plan:** Команда `python -m git_cli --help` запускается без ошибок; tree репозитория содержит pyproject.toml, src/, tests/, README.md.

### ⬜ 2. Реализовать core-модуль git_client.py

- **ID:** `sub-2`
- **Profile:** `20razrab1`
- **Status:** `pending`
- **Description:** Реализовать класс GitClient с методами-обёртками над subprocess.run для базовых операций: status, diff, log, add, commit, branch, checkout, pull, push, fetch. Каждый метод возвращает dataclass GitResult (returncode, stdout, stderr). Корректная обработка ошибок и таймаутов.
- **Test plan:** Юнит-тесты для каждого метода с моком subprocess.run: проверка вызываемых аргументов, возврат GitResult, проброс исключений при ненулевом returncode.
- **Dependencies:** `sub-1`

### ⬜ 3. Реализовать CLI-интерфейс на argparse

- **ID:** `sub-3`
- **Profile:** `30razrab2`
- **Status:** `pending`
- **Description:** Создать cli.py с использованием argparse: подкоманды status, diff, log, add, commit, branch, checkout, pull, push. Каждая подкоманда маппится на соответствующий метод GitClient. Коды выхода: 0=ok, 1=git error, 2=usage error.
- **Test plan:** Запуск `python -m git_cli status`, `python -m git_cli log -n 5`, `python -m git_cli diff` в реальном репозитории возвращает корректный вывод; --help отображает все подкоманды.
- **Dependencies:** `sub-2`

### ⬜ 4. Написать интеграционные тесты

- **ID:** `sub-4`
- **Profile:** `30razrab2`
- **Status:** `pending`
- **Description:** Добавить pytest-тесты, которые создают временный git-репозиторий через git init и прогоняют полный сценарий: init -> add -> commit -> branch -> checkout -> log. Покрытие >=80% для git_client.py.
- **Test plan:** `pytest --cov=git_cli --cov-report=term-missing` показывает >=80% coverage; все тесты проходят; интеграционный сценарий завершается успешно.
- **Dependencies:** `sub-3`


---
*Generated by Hermes Orchestrator*
